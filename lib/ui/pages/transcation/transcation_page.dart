import 'package:flutter/material.dart';

class TranscationPage extends StatelessWidget {
  const TranscationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Transcastion Page'),
    );
  }
}
